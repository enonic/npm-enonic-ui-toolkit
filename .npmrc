registry=https://registry.npmjs.org/

access=public
tag=latest

save-exact=false
save-prefix=~
strict-peer-deps=true

audit-level=moderate
fund=false

# Versions are set by `pnpm version:set`, which writes every package at once.
git-tag-version=false
tag-version-prefix="v"
